-- Initialization script for PostgreSQL
-- This script runs automatically on the first boot of the Postgres container.
-- The actual tables will be created by Tortoise ORM on application startup, 
-- but you can use this file for any raw SQL seeding or extensions if needed.

-- CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
